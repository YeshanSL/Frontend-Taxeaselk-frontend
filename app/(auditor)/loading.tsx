import LoadingScreen from "@/components/layout/LoadingScreen";

// Same as (business)/loading.tsx, scoped to the auditor portal.
export default function AuditorLoading() {
  return <LoadingScreen />;
}
